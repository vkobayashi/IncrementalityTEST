% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/select_metric.R
\name{metric_differences}
\alias{metric_differences}
\title{Pair experiment groups and calculate metric differences}
\usage{
metric_differences(
  data,
  metric,
  id_col = "experiment",
  group_col = "group",
  control = "control",
  treatment = "treatment",
  direction = c("treatment-control", "control-treatment"),
  na_action = c("error", "omit")
)
}
\arguments{
\item{data}{A data frame containing experiment, group, and metric columns.}

\item{metric}{Character scalar naming the metric column.}

\item{id_col}{Character scalar naming the experiment identifier column.}

\item{group_col}{Character scalar naming the group column.}

\item{control}{Value identifying the control group.}

\item{treatment}{Value identifying the treatment group.}

\item{direction}{Difference direction: \code{"treatment-control"} or
\code{"control-treatment"}.}

\item{na_action}{How to handle missing metric values: \code{"error"} or \code{"omit"}.}
}
\value{
A data frame with the experiment ID, control and treatment values,
and a \code{difference} column.
}
\description{
For every experiment, pairs one control value with one treatment value and
computes \code{treatment - control} by default. Each experiment must contain
exactly one row for each requested group.
}
\examples{
results <- data.frame(
  experiment = rep(c("A", "B", "C"), each = 2),
  group = rep(c("control", "treatment"), 3),
  RPU = c(10, 11, 8, 9.5, 12, 11.5)
)
metric_differences(results, "RPU")
}
